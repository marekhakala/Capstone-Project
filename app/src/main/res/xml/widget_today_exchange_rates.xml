<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider
    xmlns:android="http://schemas.android.com/apk/res/android"

    android:previewImage="@drawable/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen|keyguard"

    android:initialLayout="@layout/widget_today_exchange_rates"
    android:initialKeyguardLayout="@layout/widget_today_exchange_rates"

    android:minHeight="@dimen/exchange_rates_height"
    android:minWidth="@dimen/exchange_rates_width"
    android:minResizeHeight="@dimen/exchange_rates_resize_height"
    android:minResizeWidth="@dimen/exchange_rates_resize_width"

    android:updatePeriodMillis="@string/widget_update_period_millis" />